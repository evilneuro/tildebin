#!/bin/sh

if [ $# = 0 ]
then
	for i in *.html
	do
		echo -n "$i: "
		fromdos < $i > `basename $i .html`
		mv `basename $i .html` $i
		echo "done"
	done
else
	for i in $*
	do
		echo -n "$i: "
		fromdos < $i > convert.$$
		mv convert.$$ $i
		echo "done"
	done
fi
